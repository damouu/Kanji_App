<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mouad.kanjiapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kanjiapplogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kanjiapplogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondScreen" />
        <activity android:name=".ThirdActivity" />
        <activity
            android:name=".Learning_Kanji"
            android:label="@string/title_activity_learning__kanji"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Learn_Kanji" />
        <activity android:name=".Lesson_Kanji"></activity>
    </application>

</manifest>